export default function Loading() {
  return (
    <div className="min-h-screen bg-paper flex items-center justify-center">
      <span className="inline-block w-5 h-5 border-2 border-ink/30 border-t-ink/80 rounded-full animate-spin" />
    </div>
  );
}
